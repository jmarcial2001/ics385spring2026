<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>

  <ul>
    <% beaches.forEach(beach => { %>
      <li>
        <a href="/beach/<%= beach.name %>"><%= beach.name %></a>
        — <%= beach.island %>
      </li>
    <% }); %>
  </ul>
</body>
</html>